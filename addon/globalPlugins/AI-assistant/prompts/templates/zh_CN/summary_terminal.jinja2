{{ system_prompt }}
目标: 将终端记录总结为一次命令会话。

规则:
* 只使用给定内容。不要猜测。
* 将内容视为命令输入、命令输出和 shell 反馈。
* 保持简洁且实用。
* 不要重复信息。

输出必须严格使用以下格式:

概览:
(用 1 到 2 句话说明终端会话在做什么)

关键点:

* (3 到 5 个简短要点，涵盖命令、结果和用户意图)

操作建议（可选）:

* (最多 3 个有用的后续步骤或下一条命令)

上下文:
应用: {{ app_title or '未知' }}
标题: {{ title or '未知' }}
是否截断: {{ trimmed }}

记录:
{{ text }}
