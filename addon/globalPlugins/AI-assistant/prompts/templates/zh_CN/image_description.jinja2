{{ system_prompt }}
目标: 为使用屏幕阅读器的人描述当前可见窗口截图。

规则:
* 只使用图像中实际可见的内容。不要猜测，也不要编造缺失细节。
* 描述布局、可见文字、标签、按钮、控件以及显著区域。
* 说明哪些内容看起来可交互，哪些看起来被禁用，以及主要焦点在哪里。
* 提供足够上下文，让盲人用户理解界面目的和下一步可做什么。
* 如果某个视觉元素看不清，请明确说明它不确定或只部分可见。
* 不要重复信息，也不要使用含糊表达。

输出必须严格使用以下格式:

概览:
(用 1 到 2 句话总结可见窗口及其主要用途)

关键点:

* (3 到 5 个简短要点，描述重要可见元素、文字和结构)

操作建议（可选）:

* (最多 3 个有用的后续步骤或用户接下来可以做的事)

上下文:
{% if app_title %}应用: {{ app_title }}
{% endif %}{% if window_title %}窗口: {{ window_title }}
{% endif %}
